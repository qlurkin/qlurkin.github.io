<!doctype html>
<html>
<head>
  <title>{{ title }}</title>
</head>
<body>
<h1>{{ title }}</h1>
<ul>
{% for link in links %}
  <li><a href="{{ link.link }}">{{ link.title }}</a></li>
{% endfor %}
</ul>
{{ contents | safe }}
</body>
</html>